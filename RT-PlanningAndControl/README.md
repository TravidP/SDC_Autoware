# GD-PlanningAndControl

This repo contains two folders, one related with planning and one with control.
Each folder contains a package for each vehicle type (currently only twizy and citaro).
Each package is comprised of configuration and launch files as the totality of the functionality of these two components comes from autoware.
